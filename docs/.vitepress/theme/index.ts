// Default theme + brand overrides only. Keep this file free of custom
// components so VitePress upgrades stay drop-in.
import DefaultTheme from 'vitepress/theme'
import './style.css'

export default DefaultTheme
